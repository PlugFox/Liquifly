fn main() {
    // Build script for Liquifly
    // Currently empty, but can be used for:
    // - Generating FFI bindings
    // - Platform-specific compilation flags
    // - Linking native libraries
    println!("cargo:rerun-if-changed=build.rs");
}
