name: liquifly
description: "High-performance fluid simulation with Rust backend"
publish_to: 'none'
version: 0.0.1+1

environment:
  sdk: '>=3.7.0 <4.0.0'
  flutter: '>=3.35.7 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  ffigen: ^20.0.0

flutter:
  uses-material-design: true
