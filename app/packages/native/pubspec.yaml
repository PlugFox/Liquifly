name: native
description: Native Rust FFI bindings for Liquifly
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  ffi: ^2.1.3

dev_dependencies:
  test: ^1.25.0
  ffigen: ^20.0.0
